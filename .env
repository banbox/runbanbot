# Banbot 本地运行所用的环境变量（被 docker-compose.yml 引用）
# 修改后可直接执行 `docker compose up -d`

# TimescaleDB（PostgreSQL）镜像版本
# 建议选择与本地 Docker 兼容的稳定版本（如 latest-pg17）
TIMESCALE_IMAGE=timescale/timescaledb:latest-pg17


TIMESCALE_NAME=timescaledb
POSTGRES_USER=postgres
POSTGRES_PASSWORD=123

# 数据库对外映射端口（宿主机）
# 若本地已有 PostgreSQL 占用 5432，可改为其他端口
PG_PORT=5432

# 宿主机持久化数据目录，容器内挂载为 /pgdata
PGDATA_DIR=./pgdata
BANBOT_TAG=latest
BANBOT_NAME=banbot
BANBOT_PORT=8000  # Banbot Web UI 对外映射端口（宿主机）

# 策略代码目录（宿主机路径），会挂载到容器 /ban/strats
# 建议将你的策略工程（如 banstrats）放在此目录下或使用绝对路径
BANBOT_STRAT_DIR=./strats

# 数据输出目录（宿主机路径），会挂载到容器 /ban/data
# 用于回测输出、前端资源缓存等
BANBOT_DATA_DIR=./data

# 交易所访问代理（中国大陆环境下访问币安 API 常需代理）
# 可设置为形如 http://127.0.0.1:10808；设置为 no 表示禁用系统代理
BANBOT_PROXY=no
